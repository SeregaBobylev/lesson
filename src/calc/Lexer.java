package calc;

import java.util.Stack;

public class Lexer {
    Token getNextToken;
    Token lookAhead;
    public Lexer(String expression) {
       Token token = new Token();
    }

    private static class Token{
        calc.Token.getType()
        }
}
